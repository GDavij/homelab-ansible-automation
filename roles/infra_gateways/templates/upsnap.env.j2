# Upsnap Environment Configuration
# Auto-generated by Ansible for {{ inventory_hostname }}
# Note: Upsnap primarily uses command-line arguments, not environment variables

# Container Configuration
UPSNAP_DATA_DIR={{ upsnap_config.data_dir }}

# Network Configuration (for reference)
UPSNAP_BIND_ADDRESS={{ upsnap_config.bind_address }}
UPSNAP_WEB_PORT={{ upsnap_config.web_port }}

# Wake-on-LAN Configuration (for reference)
UPSNAP_WOL_BROADCAST={{ upsnap_config.wol_settings.broadcast_address }}
UPSNAP_WOL_PORT={{ upsnap_config.wol_settings.magic_packet_port }}

# SSH Configuration (for reference)
UPSNAP_SSH_TIMEOUT={{ upsnap_config.ssh_timeout }}

# Network Scanning (for reference)
UPSNAP_SCAN_RANGE={{ upsnap_config.scan_range }}

# Logging Configuration
UPSNAP_LOG_LEVEL=info