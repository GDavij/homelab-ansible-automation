---
- name: Load Network and IPVS Kernel
  community.general.modprobe:
    name: "{{ item }}"
    state: present
  loop:
    # Netorking Access (CNI)
    - overlay
    - br_netfilter
    # IPVS (High-Performance Load Balancing)
    - ip_vs
    - ip_vs_rr
    - ip_vs_wrr
    - ip_vs_sh
    # Connection Tracking (Vital for Kube-Proxy)
    - nf_conntrack
  become: true

- name: Persist Modules on the Boot (/etc/modules-load.d/k8s.conf)
  ansible.builtin.copy:
    dest: /etc/modules-load.d/k8s.conf
    content: |
      overlay
      br_netfilter
      ip_vs
      ip_vs_rr
      ip_vs_wrr
      ip_vs_sh
      nf_conntrack
    mode: '0644'
  become: true
